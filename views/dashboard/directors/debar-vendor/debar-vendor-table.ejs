<%- include('../../../partials/header'); -%>

<%- include('../../../partials/sidebar'); -%>


<div class="main-panel">
  <!-- Navbar area -->
  <%- include('../../../partials/navbar'); -%>

  <div class="content">

    <div class="container-fluid">
      <div class="row">
        <div class="col-md-12">
          <div class="card">
            <div class="card-header card-header-primary">
              <h4 class="card-title ">Yearly Debar Vendor Lists</h4>
              <p class="card-category">Lists all yearly debar vendor list forms</p>
              <a href="/dashboard/directors/debar-vendor?new=true" type="button" id="add-btn" class="btn btn-light pull-right btn-sm">Add New Form</a>
            </div>
            <div class="card-body">
              <div class="table-responsive">
                <table class="table data-table">
                  <thead class="text-primary">
                    <th class="text-bold">
                      Year
                    </th>
                    <th class="text-bold">
                      Last Filled Quarter
                    </th>
                    <th class="text-bold">
                      Total Quarters Filled
                    </th>
                    <th class="text-bold">
                      Director Name
                    </th>
                    <th class="text-bold">
                      Actions
                    </th>
                  </thead>
                  <tbody>
                    <% for(form of forms){ %>
                    <tr>
                      <td>
                        <%= form.year %>
                      </td>
                      <td>
                        <%= form.quarters[form.quarters.length - 1].quarter %>
                      </td>
                      <td>
                        <%= form.quarters.length %>
                      </td>
                      <td>
                        <%= form.director.firstName +' '+form.director.lastName %>
                      </td>
                      <td>
                        <a href="/dashboard/directors/debar-vendor/<%= form._id %>" rel="tooltip" data-original-title="View Form"  class="btn btn-link btn-info btn-just-icon"><i class="material-icons">description</i></a>
                          <a href="/dashboard/directors/debar-vendor?edit=true&id=<%= form._id %>" rel="tooltip" data-original-title="Add Quarter Record" class="btn btn-link btn-warning btn-just-icon"><i class="material-icons">edit</i></a>
                        <a href="#" rel="tooltip" class="btn btn-link btn-danger btn-just-icon remove"><i class="material-icons">close</i></a>
                      </td>
                    </tr>
                  <% } %>
                  </tbody>
                  
                </table>
              </div>
            </div>
          </div>
        </div>
    
      </div>
    
    </div>
  
  
  </div>



  <!-- Bottom footer -->
  <%- include('../../../partials/bottombar'); -%>
</div>

<!-- page footer -->
<%- include('../../../partials/footer'); -%>